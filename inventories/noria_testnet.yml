---
all:
  vars:
    #TODO: decide how and where to declare toml files
    minimum_gas_prices: "0.0025unoria,0.0025ucrd"
    chain_denom: "unoria"
    genesis_json_url: "TBH"
    persistent_peers: "TBH"
    node_user: noria
    use_cosmovisor: true
    #general settings
    snapshot_rpc: TBH
    chain_folder_name: ".noria"
    chain_binary_name: noriad
    chain_repository: TBH
    chain_home_clear: false
    node_unsafe_reset: false
    node_service_name: "noriad"
    chain_denom: unoria
    chain_version: v1.0.0 (TBH)
    # default configuration
    cosmovisor_service_name: "cosmovisor"
    cosmovisor_skip_backup: false
    chain_id: "oasis-3"
    # go configuration
    go_arch: "{{binary_arch_map[ansible_architecture]}}"
    go_version: 1.19
    binary_arch_map:
      aarch64: "linux-arm64"
      x86_64: "linux-amd64"

  children:
    node:
      hosts: "{{ target }}"


